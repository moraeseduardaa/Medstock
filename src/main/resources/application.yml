spring:
  data:
    mongodb:
      uri: mongodb://admin:admin123@localhost:27017/medstock?authSource=admin
  application:
    name: medstock

server:
  port: 8080

springdoc:
  swagger-ui:
    path: /docs